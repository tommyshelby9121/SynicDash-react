{
  "name": "@synicdash/server",
  "version": "1.0.0",
  "description": "Server-side application for SynicBot Dashboard",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
